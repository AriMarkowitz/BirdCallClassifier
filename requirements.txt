# PyTorch (CUDA 12.1) — install via:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.5.0
torchvision
torchaudio

# Model backbone
timm>=1.0.0

# Training
pytorch_lightning>=1.9.0,<2.0
torchmetrics

# setuptools <81 needed for pkg_resources (required by pytorch_lightning 1.x)
setuptools<81

# Audio processing
librosa>=0.11.0
soundfile>=0.13.0

# Data & evaluation
pandas
scikit-learn
scipy
numpy

# Logging
tensorboard
tqdm
wandb
